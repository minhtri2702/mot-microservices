server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/account_db?TimeZone=UTC
    username: mot_admin
    password: "0000"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate.jdbc.time_zone: UTC
